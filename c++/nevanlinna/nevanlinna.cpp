#include <cmath>
#include "./nevanlinna.hpp"

namespace nevanlinna {

  template <typename T>
  nda::array<double, 2> solver<T>::solve(const nda::array<double, 2> &input) {
    return nda::array<double, 2>();
  }
} // namespace nevanlinna
